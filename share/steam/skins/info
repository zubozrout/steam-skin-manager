In "no-buttons" folder there are skins prepaired for native window border. They have no window-buttons.

In "with-buttons" folder there are skins with window-buttons, so they can be used with Steam by default.

